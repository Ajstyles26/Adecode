<CascadingAuthenticationState>
    <Router AppAssembly="@typeof(App).Assembly">
        <Found Context="routeData">
            <AuthorizeRouteView RouteData="@routeData" DefaultLayout="@typeof(MainLayout)">
                <NotAuthorized>
                    <p role="alert">You dont have permissions to view this page</p>
                    <MudButton Href="/" StartIcon="@Icons.TwoTone.Home" Color="Color.Primary">Go to Home Page</MudButton>
                </NotAuthorized>
            </AuthorizeRouteView>
            <FocusOnNavigate RouteData="@routeData" Selector="h1"/>
        </Found>
        <NotFound>
            <PageTitle>Not found</PageTitle>
            <LayoutView Layout="@typeof(MainLayout)">
                <p role="alert">Sorry, there's nothing at this address.</p>
                <MudButton Href="/" Color="Color.Primary">Go to Home Page</MudButton>
            </LayoutView>
        </NotFound>
    </Router>
</CascadingAuthenticationState>
